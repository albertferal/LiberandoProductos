apiVersion: v1
kind: Service
metadata:
  name: {{ include "liberando-productos.fullname" . }}
  labels:
    {{- include "liberando-productos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liberando-productos.selectorLabels" . | nindent 4 }}
