apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liberando-productos.fullname" . }}-test-connection"
  labels:
    {{- include "liberando-productos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liberando-productos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
